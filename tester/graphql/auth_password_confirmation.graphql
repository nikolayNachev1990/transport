mutation tester_auth_password_confirmation(
  $token: String!
  $password: String!
  $password_confirmation: String!
) {
  auth_password_confirmation(
    token: $token
    password: $password
    password_confirmation: $password_confirmation
  ) {
    success
    data
  }
}

query tester_members_by_company($companyId: uuid!) {
  members(where: { company_id: { _eq: $companyId } }) {
    user_id
    company_id
    company_role
    is_active
    is_creator
    created_by
  }
}
